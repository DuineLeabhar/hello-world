git add .
(that's to add everything - can do singly, e.g. git add hello.txt)

git commit -m "your message"

git push

(can also use git pull - e.g. if someone else has updated the online git repository and you want to synchronise your computer with the online repository)


git status